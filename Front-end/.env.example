# URL of your hosted backend server API. 
# In production, if served statically from the backend on the same domain, leave this empty (it defaults to '/api').
# If the frontend is hosted on a separate domain (e.g. Vercel), set this to your hosted backend's URL:
# VITE_API_URL=https://my-backend-domain.com/api
VITE_API_URL=https://resumescore-1-lx9t.onrender.com
